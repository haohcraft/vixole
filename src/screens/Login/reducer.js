const initialState = {
    user: {
        isLogin: false,
        fb: {}
    }
};
const user = (state = initialState, action = {}) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;
